<%= javascript_packs_with_chunks_tag 'myTrips' %>
<%= stylesheet_packs_with_chunks_tag 'home' %>

<!DOCTYPE html>
<html>
<head>
  <title>My Trips</title>
</head>
<body>
  <h1>My Trips</h1>
  <div id="tripList">
    <!-- Properties will be dynamically populated here -->
  </div>

  <script>
    // Assume propertiesData contains the booked properties data
    const propertiesData = [
      { id: 1, title: 'Property 1', city: 'City 1', country: 'Country 1', image_url: 'image1.jpg' },
      { id: 2, title: 'Property 2', city: 'City 2', country: 'Country 2', image_url: 'image2.jpg' },
      // Add more properties as needed
    ];

    // Populate the tripList div with booked properties
    const tripList = document.getElementById('tripList');
    propertiesData.forEach(property => {
      const propertyDiv = document.createElement('div');
      propertyDiv.innerHTML = `
        <h3>${property.title}</h3>
        <p>Location: ${property.city}, ${property.country}</p>
        <img src="${property.image_url}" alt="${property.title}" width="200">
        <a href="/properties/${property.id}">View Details</a>
        <hr>
      `;
      tripList.appendChild(props.children);
    });
  </script>
</body>
</html>